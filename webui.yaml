apiVersion: apps/v1
kind: Deployment
metadata:
  name: react
  labels:
    app: react
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
      - name: react
        image: bxdda/react:latest
        ports:
        - containerPort: 80
        env:
          - name: API_KEY
            value: ${{ secrets.API_KEY }}
---
apiVersion: v1
kind: Service
metadata:
  name: react
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30088
  selector:
    app: react
